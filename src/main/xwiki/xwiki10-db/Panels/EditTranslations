<?xml version="1.0" encoding="UTF-8"?>

<xwikidoc>
<web>Panels</web>
<name>EditTranslations</name>
<language></language>
<defaultLanguage>en</defaultLanguage>
<translation>0</translation>
<parent></parent>
<creator>XWiki.Admin</creator>
<author>XWiki.Admin</author>
<contentAuthor>XWiki.Admin</contentAuthor>
<creationDate>1154089417000</creationDate>
<date>1154184154000</date>
<contentUpdateDate>1154184154000</contentUpdateDate>
<version>1.17</version>
<title></title>
<template></template>
<content>#if ($xwiki.isMultiLingual()==true)
#largepanelheader($msg.get("Translations"))
#if(!$tdoc.language || $tdoc.language == "" || $tdoc.language == $doc.defaultLanguage)
&lt;div&gt;&lt;strong&gt;$xwiki.parseMessage("youareeditingoriginal") (${doc.defaultLanguage}).&lt;/strong&gt;&lt;/div&gt;
#if($!doc.translationList.size() &gt; 0)
&lt;div&gt;$msg.get("existingtranslations"):
#foreach($translation in $doc.translationList)
&lt;a href="$doc.getURL("edit", "editor=${editor}&amp;amp;language=${translation}")"&gt;$translation&lt;/a&gt;
#end
&lt;/div&gt;
#end
#else
&lt;div&gt;&lt;strong&gt;$msg.get("youareeditingtranslation"): ${tdoc.language}.&lt;/strong&gt;&lt;/div&gt;
&lt;div&gt;$msg.get("originallanguage") &lt;a href="$doc.getURL("edit", "editor=${editor}&amp;amp;language=${doc.defaultLanguage}")"&gt;${doc.defaultLanguage}&lt;/a&gt;.&lt;/div&gt;
#if($doc.translationList.size() &gt; 1)
&lt;div&gt;$msg.get("othertranslations"):
#foreach($translation in $doc.translationList)
#if($translation != $tdoc.language)
&lt;a href="$doc.getURL("edit", "editor=${editor}&amp;amp;language=${translation}")"&gt;$translation&lt;/a&gt;
#end
#end
&lt;/div&gt;
#end
#end
&lt;div&gt;
$msg.get("translatedocin"):
#set($elangs = ",")
#foreach($elang in $doc.translationList)
#set($elangs = $elangs.concat($elang).concat(","))
#end
#foreach($olang in $xwiki.getXWikiPreference("languages").split(","))
#if(($elangs.indexOf(",$olang,") == -1) &amp;&amp; ($olang != $doc.defaultLanguage))
&lt;a href="$doc.getURL("edit", "language=${olang}")"&gt;$olang&lt;/a&gt;
#end
#end
&lt;/div&gt;
#panelfooter()
#end</content>
</xwikidoc>
