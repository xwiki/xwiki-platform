{
  "name": "@cristal/electron-preload",
  "version": "0.7.0",
  "license": "AGPL-3.0",
  "repository": {
    "type": "git",
    "directory": "electron/preload",
    "url": "https://github.com/xwiki-contrib/cristal/"
  },
  "scripts": {
    "build": "pnpm compile",
    "clean": "rimraf dist",
    "compile": "tsc --project tsconfig.json --noEmit && vite build",
    "electron": "echo 'no task'",
    "lint": "eslint \"./src/**/*.{ts,tsx}\" --max-warnings=0",
    "linux": "echo 'no task'",
    "prepack": "pnpm build",
    "package": "echo 'no task'",
    "test": "echo 'no test'"
  },
  "devDependencies": {
    "@cristal/browser-electron": "workspace:*",
    "@cristal/electron-storage": "workspace:*",
    "@types/node": "20.12.12",
    "rimraf": "5.0.7",
    "typescript": "5.4.5",
    "unplugin-auto-expose": "0.2.4",
    "vite": "5.2.11"
  }
}
