<h2>Properties</h2>
<table border="0">
#foreach($prop in $doc.xWikiObject.xWikiClass.properties)
<tr><td>$prop.prettyName</td>
<td>$prop.displayView($prop.getName(), "object_",  $doc.xWikiObject, $context)
</td></tr>
#end
#foreach($obj in $doc.xWikiObjects)
<tr><td>$obj.xWikiClass.name</td></tr>
#foreach($prop in $obj.xWikiClass.properties)
<tr><td>$prop.prettyName</td>
<td>$prop.displayView($prop.getName(), "${obj.xWikiClass.name}_",  $obj, $context)
</td></tr>
#end
#end
</table>
