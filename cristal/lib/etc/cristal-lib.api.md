## API Report File for "@xwiki/cristal-lib"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { App } from 'vue';
import { BrowserApi } from '@xwiki/cristal-browser-api';
import { Component } from 'vue';
import { Configuration } from '@xwiki/cristal-configuration-api';
import { ConfigurationLoader } from '@xwiki/cristal-configuration-api';
import { Configurations } from '@xwiki/cristal-configuration-api';
import { Container } from 'inversify';
import { CristalApp } from '@xwiki/cristal-api';
import { CristalLoader } from '@xwiki/cristal-extension-manager';
import { ExtensionManager } from '@xwiki/cristal-extension-manager';
import { Logger } from '@xwiki/cristal-api';
import { LoggerConfig } from '@xwiki/cristal-api';
import { PageData } from '@xwiki/cristal-api';
import { Ref } from 'vue';
import { Router } from 'vue-router';
import { SkinManager } from '@xwiki/cristal-api';
import { VueTemplateProvider } from '@xwiki/cristal-skin';
import { WikiConfig } from '@xwiki/cristal-api';

// @beta
export function conditionalComponentsList(container: Container, configuration: Configuration): Promise<void>;

// @beta (undocumented)
export class CristalAppLoader extends CristalLoader {
    constructor(extensionList: Array<string>);
    // (undocumented)
    cristal: DefaultCristalApp;
    // (undocumented)
    static init(extensionList: Array<string>, loadConfig: ConfigurationLoader, staticBuild: boolean, isElectron: boolean, configName: string,
    additionalComponents: (container: Container) => Promise<void>,
    conditionalComponents: (container: Container, config: Configuration) => Promise<void>): void;
    // (undocumented)
    launchApp(forceStaticMode: boolean, loadConfig: ConfigurationLoader, isElectron: boolean, configName: string,
    additionalComponents: (container: Container) => Promise<void>,
    conditionalComponents: (container: Container, config: Configuration) => Promise<void>): Promise<void>;
    // (undocumented)
    loadApp(config: Configurations, isElectron: boolean, configName: string): Promise<void>;
}

// @beta
export function defaultComponentsList(container: Container): Promise<void>;

// @beta (undocumented)
export class DefaultCristalApp implements CristalApp {
    constructor(extensionManager: ExtensionManager, skinManager: SkinManager, vueTemplateProviders: VueTemplateProvider[], logger: Logger, browserApi: BrowserApi);
    // (undocumented)
    app: App;
    // (undocumented)
    availableConfigurations: Map<string, WikiConfig>;
    // (undocumented)
    browserApi: BrowserApi;
    // (undocumented)
    container: Container;
    // (undocumented)
    currentContentRef: Ref;
    // (undocumented)
    deleteAvailableConfiguration(configName: string): void;
    // (undocumented)
    extensionManager: ExtensionManager;
    // (undocumented)
    getApp(): App;
    // (undocumented)
    getAvailableConfigurations(): Map<string, WikiConfig>;
    // (undocumented)
    getContainer(): Container;
    // (undocumented)
    getCurrentContent(): string;
    // (undocumented)
    getCurrentPage(): string;
    // (undocumented)
    getCurrentSource(): string;
    // (undocumented)
    getCurrentSyntax(): string;
    // (undocumented)
    getCurrentWiki(): string;
    // (undocumented)
    getLogger(module: string): Logger;
    // (undocumented)
    getLoggerConfig(): LoggerConfig;
    // (undocumented)
    getMenuEntries(): Array<string>;
    // (undocumented)
    getPage(page: string, options?: {
        requeue?: boolean;
        revision?: string;
    }): Promise<PageData | undefined>;
    // (undocumented)
    getPageFromHash(hash: string): string | null;
    // (undocumented)
    getRouter(): Router;
    // (undocumented)
    getSkinManager(): SkinManager;
    // (undocumented)
    getUIXTemplates(extensionPoint: string): Promise<Array<Component>>;
    // (undocumented)
    getWikiConfig(): WikiConfig;
    // (undocumented)
    handlePopState(name: string, revision?: string): void;
    // (undocumented)
    isElectron: boolean;
    loadPage(options?: {
        requeue: boolean;
    }): Promise<void>;
    // @deprecated (undocumented)
    loadPageFromURL(url: string): Promise<void>;
    // (undocumented)
    logger: Logger;
    // (undocumented)
    mode: string;
    // (undocumented)
    page: PageData;
    // (undocumented)
    router: Router;
    // (undocumented)
    run(): Promise<void>;
    // (undocumented)
    setAvailableConfigurations(configs: Configurations): void;
    // (undocumented)
    setContainer(container: Container): void;
    // (undocumented)
    setContentRef(ref: Ref): void;
    // (undocumented)
    setCurrentPage(newPage: string, mode?: string): Promise<void>;
    // (undocumented)
    setWikiConfig(wikiConfig: WikiConfig): void;
    // (undocumented)
    skinManager: SkinManager;
    // (undocumented)
    switchConfig(configName: string): void;
    // (undocumented)
    vueTemplateProviders: VueTemplateProvider[];
    // (undocumented)
    wikiConfig: WikiConfig;
}

// (No @packageDocumentation comment for this package)

```
