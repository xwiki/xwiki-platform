## API Report File for "@xwiki/platform-livedata-api"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { Reactive } from 'vue';
import { Ref } from 'vue';

// @beta
export interface ActionDescriptor {
    allowProperty?: string;
    id: string;
}

// @beta
export interface Data {
    count: number;
    entries: Values[];
}

// @beta
export interface DisplayerDescriptor {
    id: string;
}

// @beta
export interface EntryDescriptor {
    idProperty: string;
}

// @beta
export interface Filter {
    constraints: QueryConstraint[];
    matchAll: boolean;
    property: string;
}

// @beta
export interface FilterDescriptor {
    defaultOperator: string;
    id: string;
    operators: OperatorDescriptor[];
}

// @beta
export interface LayoutDescriptor {
    id: string;
}

// @beta
export interface LiveDataSource {
    getEntries(query: Query): Promise<Data>;
    updateEntry(source: Source, entryId: string, values: unknown): Promise<void>;
    updateEntryProperty(source: Source, entryId: string, propertyId: string, value: unknown): Promise<void>;
}

// @beta
export interface Logic {
    addFilter(property: string, operator: unknown, value: string, index: number): Promise<void>;
    addSort(property: string, descending: boolean | undefined): Promise<void>;
    changeLayout(layoutId: string): void;
    currentLayoutId?: Ref<string>;
    data?: Reactive<LogicData>;
    filter(property: string, index: number, filterEntry: {
        index: number;
    }, input: {
        filterOperator?: unknown;
        skipFetch?: boolean;
    }): Promise<void>;
    getEntryId(entry: Values): string | undefined;
    getPageCount(): number;
    isContentTrusted(): boolean;
    onEvent(event: string, callback: (e: Event) => void): void;
    onEventWhere(eventName: string, condition: object | ((p: unknown) => boolean), callback: (e: Event) => void): void;
    registerPanel(panel: Panel): void;
    removeFilter(property: string, index: number): Promise<void>;
    removeSort(property: string): Promise<void>;
    reorderSort(propertyId: string, toIndex: number): void;
    setElement(element: HTMLElement): void;
    setValues(input: {
        entryId: string;
        values: unknown;
    }): Promise<unknown>;
    sort(property: string, level: number, descending?: boolean): Promise<void>;
    translationsLoaded(): Promise<boolean>;
    triggerEvent(eventName: string, eventData?: object): void;
    updateEntries(): Promise<void>;
}

// @beta
export interface LogicData {
    data: Data;
    id: number;
    meta: Meta;
    query: Query;
}

// @beta
export interface Meta {
    actions: ActionDescriptor[];
    defaultDisplayer: string;
    defaultLayout: string;
    displayers: DisplayerDescriptor[];
    entryDescriptor: EntryDescriptor;
    filters: FilterDescriptor[];
    layouts: LayoutDescriptor[];
    propertyDescriptors: PropertyDescriptor_2[];
    propertyTypes: PropertyDescriptor_2[];
    selection: {
        enabled: boolean;
    };
}

// @beta
export interface OperatorDescriptor {
    id: string;
    name: string;
}

// @beta
export interface Panel {
    component: string;
    container?: Element;
    icon: string;
    id: string;
    name: string;
    order: number;
    title: string;
}

// @beta
interface PropertyDescriptor_2 {
    displayer: DisplayerDescriptor;
    editable?: boolean;
    filter: Filter;
    filterable?: boolean;
    id: string;
    sortable?: boolean;
    type: string;
    visible?: boolean;
}
export { PropertyDescriptor_2 as PropertyDescriptor }

// @beta
export interface Query {
    filters: Filter[];
    limit: number;
    offset: number;
    properties: string[];
    sort: SortEntry[];
    source: Source;
}

// @beta
export interface QueryConstraint {
    operator: string;
    value: unknown;
}

// @beta
export interface SortEntry {
    descending: boolean;
    property: string;
}

// @beta
export type Source = {
    id: string;
} & {
    [key: string]: string;
};

// @beta
export type TranslationQuery = {
    prefix: string;
    keys: string[];
};

// @beta
export type Translations = {
    [p: string]: string;
};

// @beta
export type Values = {
    [key: string]: string;
};

// (No @packageDocumentation comment for this package)

```
