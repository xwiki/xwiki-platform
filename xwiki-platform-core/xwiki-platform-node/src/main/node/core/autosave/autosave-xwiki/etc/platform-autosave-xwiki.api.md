## API Report File for "@xwiki/platform-autosave-xwiki"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { Logger } from '@xwiki/platform-api';
import { Saver } from '@xwiki/platform-autosave-api';
import { SaveResult } from '@xwiki/platform-autosave-api';
import { SaverState } from '@xwiki/platform-autosave-api';
import { SaveTarget } from '@xwiki/platform-autosave-api';
import { XWikiDocument } from '@xwiki/platform-document-xwiki';

// @beta
export type DocumentVersion = {
    number: string;
    date: number;
    author?: string | DocumentVersionAuthor;
};

// @beta
export type DocumentVersionAuthor = {
    reference?: string;
    name?: string;
};

// @beta
export type XWikiFormSaveContext = {
    button?: HTMLInputElement;
};

// @beta
export class XWikiFormSaveTarget extends SaveTarget<XWikiFormSaveContext> {
    constructor(saver: Saver<XWikiFormSaveContext>, config: XWikiFormSaveTargetConfig);
    // (undocumented)
    dispose(): void;
    // (undocumented)
    getSaveButton(continueEditing?: boolean): HTMLInputElement | null;
    // (undocumented)
    getSavePriority(input: XWikiFormSaveContext): number;
    // (undocumented)
    initialize(): Promise<void>;
    // (undocumented)
    onStatesChanged(states: Record<string, SaverState>, localClientId: string): void;
    // (undocumented)
    submit(input: XWikiFormSaveContext): Promise<SaveResult>;
}

// @beta
export type XWikiFormSaveTargetConfig = {
    document: XWikiDocument;
    formId?: string;
    autoSaveVersionSummary?: string;
    onCreateVersion?: (version: DocumentVersion) => void;
    logger?: Logger;
};

// (No @packageDocumentation comment for this package)

```
