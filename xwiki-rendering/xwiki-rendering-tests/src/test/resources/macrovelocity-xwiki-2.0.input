{velocity}{xhtml}
<div>
  #set ($name = "John")
  #set ($list = ['one', 'two'])
  <h1>Hello $name</h1>
  <table>
    #foreach ($item in $list)
      <tr>
        <td>
          * item
        </td>
      </tr>
    #end
  </table>
</div>
{/xhtml}{/velocity}