##syntax=xwiki/2.1
{{velocity}}
## We use a hack to insert the new line in the translation parameters.
$services.localization.render('platform.extension.distributionWizard.welcomeStepDescription', ['
* ', '
* '])

(% class="steps-hint" %)
{{translation key="platform.extension.distributionWizard.welcomeStepStepsHint"/}}

#set ($stepGoals = {
  'extension.defaultui': $services.localization.render('platform.extension.distributionWizard.uiStepSummary'),
  'extension.outdatedextensions': $services.localization.render('platform.extension.distributionWizard.extensionsStepSummary')
})
(% class="steps vertical" %)
#foreach ($step in $steps)
  * (% class="number" %)$velocityCount(%%)##
    (% class="name" %)$stepNames.get($step.id)(%%)##
    (% class="description" %)$stepGoals.get($step.id)
#end

(% class="xHint" %)
{{translation key="platform.extension.distributionWizard.welcomeStepActionsHint"/}}

#set ($showCompleteStepButton = true)
#set ($canCloseWizard = true)
{{/velocity}}
