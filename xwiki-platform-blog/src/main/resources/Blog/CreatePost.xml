<?xml version="1.0" encoding="UTF-8"?>

<xwikidoc>
<web>Blog</web>
<name>CreatePost</name>
<language></language>
<defaultLanguage>en</defaultLanguage>
<translation>0</translation>
<parent>Blog.WebHome</parent>
<creator>XWiki.Admin</creator>
<author>XWiki.Admin</author>
<customClass></customClass>
<contentAuthor>XWiki.Admin</contentAuthor>
<creationDate>1227218107000</creationDate>
<date>1227681885000</date>
<contentUpdateDate>1227681885000</contentUpdateDate>
<version>1.1</version>
<title>Create a new blog post</title>
<template></template>
<defaultTemplate></defaultTemplate>
<validationScript></validationScript>
<comment></comment>
<minorEdit>false</minorEdit>
<syntaxId>xwiki/1.0</syntaxId>
<hidden>true</hidden>
<content>#includeMacros("Blog.BlogCode")
#if($hasEdit)
$xwiki.jsx.use($blogScriptsDocumentName)##
##
##
##
#set($name = "$!request.entryTitle")
#if($name == '')
  ## First step, display the create form
  #set($space = "$!request.entrySpace")
  #if($space == '')
    #set($space = $doc.space)
  #end
  1.1 $msg.get("xe.blog.post.createpost")
  &lt;form action="${xwiki.getURL('Blog.CreatePost')}" id="newBlogPost" method="post"&gt;
  &lt;div&gt;
    &lt;input type="hidden" name="entrySpace" value="$space"/&gt;
    &lt;input type="text" name="entryTitle" value="$msg.get("xe.blog.post.title")"/&gt;
    &lt;input type="submit" value="$msg.get("xe.blog.post.create")"/&gt;
  &lt;/div&gt;
  &lt;/form&gt;
#else
  ## Second step, form submitted, create the document
  #set($space = "$!request.entrySpace")
  #if($space == '')
    #set($space = 'Main')
  #end
  ## Remove . and : from the document name, as they have a special meaning in XWiki document names
  #set($postDocName = "${space}.${name.replaceAll('[.:]', '')}")
  ## Get the target blog document, to set it as the parent
  #getBlogDocument($space $blogDoc)
  #set($parent = $blogDoc.fullName)
  #set($title = $util.encodeURI($name))
  $response.sendRedirect($xwiki.getURL($postDocName, 'inline', "template=${blogPostTemplate}&amp;parent=${parent}&amp;title=${title}&amp;${blogPostClassname}_0_title=${title}"))
#end## name == ''
#end## hasEdit</content>
</xwikidoc>
