#if($isInServletMode)## Visible only in a page
<div id="maineditmenu" class="actionmenu layoutsubsection">
  #xwikitopmenuentry($doc.getURL() "$services.localization.render('core.menu.editing')__SEPARATOR__$!{escapetool.xml($doc.name)}" 'tmEditing' '')
</div>
#end