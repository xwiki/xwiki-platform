<?xml version="1.0" encoding="UTF-8"?>
<xwikidoc>
<web>XWiki</web>
<name>OfficeImporterResults</name>
<language></language>
<defaultLanguage>en</defaultLanguage>
<translation>0</translation>
<parent>XWiki.OfficeImporter</parent>
<creator>XWiki.Admin</creator>
<author>XWiki.Admin</author>
<customClass></customClass>
<contentAuthor>XWiki.Admin</contentAuthor>
<creationDate>1217261425000</creationDate>
<date>1219045325000</date>
<contentUpdateDate>1219045325000</contentUpdateDate>
<version>1.1</version>
<title>Office Importer Results</title>
<template></template>
<defaultTemplate></defaultTemplate>
<validationScript></validationScript>
<comment></comment>
<minorEdit>false</minorEdit>
<syntaxId>xwiki/1.0</syntaxId>
<content>
#if($isguest)
  #error($msg.get("xe.officeimporter.notallowed"))  
#else
  1 $msg.get("xe.officeimporter.results.title")
  
  ## Validate fileName
  #set($fileUpload=$xwiki.fileupload)
  #set($fileName=$fileUpload.getFileName("filePath"))
  #if(!$fileName || $fileName=="")
  #set($goBack = "[$msg.get('xe.officeimporter.results.goback')&gt;XWiki.OfficeImporter]")
    #error($msg.get("xe.officeimporter.results.missingfile", [$goBack]))
  #end
  
  ## Validate targetSpace
  #if(!$request.targetSpace || $request.targetSpace=="")
  #set($goBack = "[$msg.get('xe.officeimporter.results.goback')&gt;XWiki.OfficeImporter]")
    #error($msg.get("xe.officeimporter.results.missingspace", [$goBack]))
  #else
    #set($targetSpace=$request.targetSpace)
  #end

  ## Validate targetPage
  #if(!$request.targetPage || $request.targetPage=="")
  #set($goBack = "[$msg.get('xe.officeimporter.results.goback')&gt;XWiki.OfficeImporter]")
    #error($msg.get("xe.officeimporter.results.missingpage", [$goBack]))
  #else
    #set($targetPage=$request.targetPage)
  #end  

  ## Check style filter settings 
  #if($request.filterStyles)
    #set($filterStyles=$request.filterStyles)
  #else
    #set($filterStyles="none")
  #end

  ## Import
  #if($fileName &amp;&amp; $targetSpace &amp;&amp; $targetPage)    
    #set($fileContent=$fileUpload.getFileItemData("filePath"))
    #set($options={"filterStyles":${filterStyles}})
    #set($targetDocument="${targetSpace}.${targetPage}")
    #set($result=$officeimporter.importDocument($fileContent, $fileName, $targetDocument, $options))
    #if($result)
    #set($goBack = "[$msg.get('xe.officeimporter.results.goback')&gt;XWiki.OfficeImporter]")
    #set($targetDocumentEscaped=$targetDocument.replaceAll(" ", "+"))
    #set($resultDocument = "*[$msg.get('xe.officeimporter.results.result')&gt;$targetDocumentEscaped]*")  
      #info($msg.get("xe.officeimporter.results.success", [$resultDocument, $goBack]))
    #else
      #error($officeimporter.lastErrorMessage)
    #end
  #end
  
#end
</content>
</xwikidoc>
