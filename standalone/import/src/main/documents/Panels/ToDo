<?xml version="1.0" encoding="ISO-8859-1"?>

<xwikidoc>
<web>Panels</web>
<name>ToDo</name>
<language></language>
<defaultLanguage>en</defaultLanguage>
<translation>0</translation>
<parent></parent>
<creator>XWiki.Admin</creator>
<author>xwiki:XWiki.LudovicDubost</author>
<customClass></customClass>
<contentAuthor>XWiki.Admin</contentAuthor>
<creationDate>1157382243000</creationDate>
<date>1157829296000</date>
<contentUpdateDate>1165751668000</contentUpdateDate>
<version>1.16</version>
<title></title>
<template></template>
<defaultTemplate></defaultTemplate>
<validationScript></validationScript>
<content>1 Stuff TO DO for the ~~Panel wizard~~

* --when generating available panels list, create and put placeholders for the panels that are already in columns--
* --save--
** --generate the list for "rightPanels" and "leftPanels" as it is in preferences--
** --write the save page--
** --move save page--
* --fix Opera offsetXXX problem--
* --fix IE... ?--
* fix the tooltip bug
* --get rid of the click bug: when you just click on a panel in the list, without moving it, it is appended at the end of the document--
* --allow the user to choose if the left/right column is visible--
** --checkboxes? __NO__--
** --something more sophisticated? __YES__--
** --fix the empty column bug--
** --make sure all columns exist in the wizard's document, no matter what the curent layout is--
** --what to do with the panels from the hidden column?? __ask Ludovic__--
* --do something about the wizard's layout, the panel list is too low--
* --__panel categories!! ask Ludovic__: Information, Navigation, Tools, Administration,  Other--
* --arrange panels in categories--
* more pages per category if necessary
* --__fix the 0 height placeholder bug__--
* --__fix the "cannot parse height" bug__--
* --put a "go home" button--
* --put a revert button--
* make sure "recently visited" is always visible
* --__handle problems generated by modifications in XWIKI Preferences__--
* __fix panel rendering in the inline form__



</content></xwikidoc>