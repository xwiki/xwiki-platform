<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.xwiki.platform</groupId>
    <artifactId>xwiki-core-xmlrpc-parent</artifactId>
    <version>1.4-milestone-2</version>
  </parent>
  <artifactId>xwiki-core-xmlrpc-client</artifactId>
  <name>XWiki Platform - Core - XMLRPC client</name>
  <packaging>jar</packaging>
  <description>XWiki Platform - Core - XMLRPC client</description>
  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <!-- These classes do not contain tests. But since they have the word 'Test' in their name
                 Surefire tries to launch them. We have to explicitly tell him to exclude them. -->
          <excludes>
            <exclude>**/AbstractXWikiXmlRpcTest.java</exclude>
            <exclude>**/TestUtils.java</exclude>
            <exclude>**/TestConstants.java</exclude>
          </excludes>
        </configuration>
      </plugin>
    </plugins>
  </build>
  <dependencies>
    <dependency>
      <groupId>org.codehaus.swizzle</groupId>
      <artifactId>swizzle-confluence</artifactId>
      <version>${swizzle.swizzle-confluence.version}</version>    
      <classifier>xwiki</classifier>
    </dependency>
    <dependency>
      <groupId>org.xwiki.platform</groupId>
      <artifactId>xwiki-core-xmlrpc-model</artifactId>
      <version>${pom.version}</version>
    </dependency>    
    <dependency>
      <groupId>org.apache.xmlrpc</groupId>
      <artifactId>xmlrpc-client</artifactId>
      <version>3.1</version>
    </dependency>
  </dependencies>
</project>