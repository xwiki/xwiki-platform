#if($context.getMode()==0)## Visible only in a page
<div id="footerglobal" class="layoutsection">
<div class="minwidth"></div>
<hr/>
  <div id="xwikidocinfo"><div class="minwidth"></div>#if ($tdoc)$msg.get("creator"): $xwiki.getLocalUserName($tdoc.creator) $msg.get("createdon") $!xwiki.formatDate($tdoc.creationDate)#end</div>
  <div id="xwikilicence">$!copyright</div>
  #set ($version = $xwiki.getWebPreference("version"))
  #if ("$!version" == "")
    #set ($version = $xwiki.getXWikiPreference("version"))
  #end
  #if ("$!version" != "")
    #set ($version = $xwiki.parseContent($version))
  #else
    #set ($version = $!xwiki.version)
  #end
  <div id="xwikiplatformversion">$version</div>
<hr/>
</div>
#end
### TODO: Format the footer for pdf output