#if ($displayer.mode == 'edit')
  <input type="text" value="$displayer.value"
  #foreach ($parameter in $displayer.parameters.entrySet()) $parameter.key="$parameter.value"#end>
#else
  $displayer.value
#end
